namespace UnityEngine.XR.ARSubsystems
{
    /// <summary>
    /// Represents the torch mode of the camera.
    /// </summary>
    public enum XRCameraTorchMode
    {
        /// <summary>
        /// The camera torch (flash) is off.
        /// </summary>
        Off = 0,

        /// <summary>
        /// The camera torch (flash) is on.
        /// </summary>
        On = 2
    }
}
